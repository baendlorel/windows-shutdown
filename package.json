{
  "name": "windows-shutdown",
  "version": "1.2.0",
  "description": "Put it on your desktop, double click it and shuts down immediately",
  "description_zh": "一个可以立刻关机的小工具，用于放在桌面上快速关机",
  "purpose": "app",
  "scripts": {
    "verbose": "cmake --build ./build --config Debug --target all -- --verbose",
    "build": "cmake -S . -B build-release -G Ninja -DCMAKE_BUILD_TYPE=Release; cmake --build build-release --config Release"
  }
}